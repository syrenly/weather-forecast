import { ToCardinalPointPipe } from './to-cardinal-point.pipe';

describe('ToCardinalPointPipe', () => {
  it('create an instance', () => {
    const pipe = new ToCardinalPointPipe();
    expect(pipe).toBeTruthy();
  });
});
